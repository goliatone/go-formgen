// Package uischema loads and applies UI schema decorators that enrich formgen
// form models with layout metadata, section assignments, and action
// definitions. The package keeps the core model builder unaware of optional UI
// overlays while providing a simple decorator that orchestrator callers can
// opt into.
package uischema
